<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smilexie.weekview.sample" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BasicActivity"
            android:label="@string/title_activity_basic" >
        </activity>
        <activity
            android:name=".AsynchronousActivity"
            android:label="@string/title_activity_asynchronous" >
        </activity>
        <activity android:name=".MonthScheduleActivity"/>
        <activity android:name=".CalendarScheduleActivity"/>
        <activity android:name=".systemcalendar.SyncSystemEventActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".systemcalendar.SyncSystemCalendarEvent"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
    </application>

</manifest>
